# راهنمای استقرار روی Vercel + Supabase + GitHub

راهنمای گام‌به‌گام برای بالا آوردن «کیف پول هوشمند» به صورت **رایگان** و **دائمی**.

زمان تقریبی: حدود ۲۰ دقیقه · هزینه: صفر · نتیجه: آدرس HTTPS دائمی قابل نصب روی آیفون

---

## 📋 پیش‌نیازها

سه حساب کاربری رایگان لازم دارید:

| سرویس | آدرس | کاربرد |
|-------|------|--------|
| GitHub | github.com | نگهداری کد |
| Supabase | supabase.com | پایگاه داده PostgreSQL |
| Vercel | vercel.com | اجرای برنامه |

> 💡 توصیه: ابتدا در GitHub ثبت‌نام کنید، سپس در Vercel و Supabase با گزینه
> **«Continue with GitHub»** وارد شوید تا همه‌چیز به هم متصل باشد.

---

## مرحله ۱ — ساخت پایگاه داده در Supabase

### ۱.۱ ساخت پروژه

1. وارد [supabase.com](https://supabase.com) شوید و روی **New Project** بزنید.
2. اطلاعات زیر را وارد کنید:
   - **Name:** `wallet`
   - **Database Password:** یک رمز قوی بسازید و **حتماً جایی ذخیره کنید** (بعداً لازم است و دیگر نمایش داده نمی‌شود)
   - **Region:** نزدیک‌ترین به ایران، مثلاً `Frankfurt (eu-central-1)`
3. روی **Create new project** بزنید و حدود ۲ دقیقه صبر کنید.

### ۱.۲ گرفتن رشته اتصال (مهم‌ترین بخش)

1. در پنل Supabase روی دکمه **Connect** در بالای صفحه بزنید
   (یا مسیر: **Project Settings → Database → Connection string**).
2. بخش **Connection pooling** را پیدا کنید.
3. حالت **Transaction** را انتخاب کنید (نه Session و نه Direct).
4. رشته‌ای شبیه این را کپی کنید:

```
postgresql://postgres.abcdefghijklmnop:[YOUR-PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
```

5. عبارت `[YOUR-PASSWORD]` را با رمز واقعی مرحله ۱.۱ جایگزین کنید.

### ⚠️ سه نکته حیاتی

**۱. حتماً پورت `6543` باشد، نه `5432`**

Vercel سرورلس است؛ یعنی برای هر درخواست یک نسخه جدید از برنامه اجرا می‌شود.
اگر از اتصال مستقیم (پورت ۵۴۳۲) استفاده کنید، خیلی زود با خطای
`too many connections` روبه‌رو می‌شوید. پورت ۶۵۴۳ همان Pooler است که برای این حالت ساخته شده.

**۲. اتصال مستقیم روی IPv6 است**

آدرس `db.xxx.supabase.co` فقط IPv6 پاسخ می‌دهد و ممکن است اصلاً وصل نشود.
آدرس Pooler روی IPv4 کار می‌کند و این مشکل را ندارد.

**۳. اگر رمز شما کاراکتر خاص دارد، باید encode شود**

| کاراکتر | معادل |
|---------|-------|
| `@` | `%40` |
| `#` | `%23` |
| `$` | `%24` |
| `&` | `%26` |
| `/` | `%2F` |
| `:` | `%3A` |

> ساده‌ترین راه: رمزی بسازید که فقط شامل حروف انگلیسی و عدد باشد.

---

## مرحله ۲ — آپلود کد در GitHub

### روش الف) با رابط گرافیکی (ساده‌تر، بدون نصب نرم‌افزار)

1. در [github.com](https://github.com) روی **New repository** بزنید.
2. نام بگذارید `wallet` و گزینه **Private** را انتخاب کنید.
3. **مهم:** تیک `Add a README file` را **نزنید**.
4. روی **Create repository** بزنید.
5. در صفحه بعد روی **uploading an existing file** کلیک کنید.
6. تمام فایل‌های پروژه را بکشید و رها کنید.

> ⛔ این موارد را آپلود **نکنید**: پوشه `node_modules`، پوشه `.next`، فایل `.env`
>
> (فایل `.gitignore` پروژه این‌ها را نادیده می‌گیرد، ولی در آپلود دستی خودتان باید حذفشان کنید.)

### روش ب) با خط فرمان Git

```bash
cd مسیر-پروژه

git init
git add .
git commit -m "نسخه اولیه کیف پول هوشمند"
git branch -M main
git remote add origin https://github.com/USERNAME/wallet.git
git push -u origin main
```

---

## مرحله ۳ — استقرار در Vercel

1. وارد [vercel.com](https://vercel.com) شوید و **Continue with GitHub** را بزنید.
2. روی **Add New → Project** کلیک کنید.
3. ریپازیتوری `wallet` را پیدا کرده و **Import** بزنید.
4. تنظیمات را **دست نزنید** — Vercel خودش Next.js را تشخیص می‌دهد:

   | فیلد | مقدار |
   |------|-------|
   | Framework Preset | Next.js ✅ |
   | Build Command | پیش‌فرض |
   | Output Directory | پیش‌فرض |

5. بخش **Environment Variables** را باز کنید و اضافه کنید:

   | Name | Value |
   |------|-------|
   | `DATABASE_URL` | رشته اتصال مرحله ۱.۲ (با پورت ۶۵۴۳) |

6. روی **Deploy** بزنید و ۲ تا ۳ دقیقه صبر کنید.

---

## مرحله ۴ — تست و ساخت خودکار جداول

پس از اتمام Deploy، این آدرس را باز کنید:

```
https://نام-پروژه.vercel.app/api/health
```

### ✅ پاسخ موفق

```json
{
  "ok": true,
  "database": "connected",
  "dialect": "postgres",
  "tablesReady": true,
  "accountCount": 21,
  "hint": "سیستم آماده استفاده است."
}
```

🎉 **جداول، ایندکس‌ها و داده‌های نمونه به صورت خودکار ساخته شدند.**
نیازی به اجرای هیچ دستور SQL یا migration نیست.

### ❌ اگر خطا دیدید

#### خطای بیلد: `Couldn't find any pages or app directory`

این رایج‌ترین خطاست و یعنی فایل‌ها داخل یک **پوشه اضافه** آپلود شده‌اند
(مثلاً `wallet-main/src/app` به جای `src/app`).

**بررسی:** در GitHub ریپازیتوری را باز کنید. باید **مستقیماً** این‌ها را ببینید:

```
📁 src
📁 scripts
📄 package.json
📄 next.config.ts
📄 tsconfig.json
```

اگر به جای این‌ها فقط **یک پوشه** می‌بینید، دو راه‌حل دارید:

**راه‌حل سریع (بدون تغییر GitHub):**
Vercel → پروژه → **Settings → General → Root Directory** → روی **Edit** بزنید →
نام آن پوشه را وارد کنید (مثلاً `wallet-main`) → **Save** → از تب Deployments، **Redeploy**.

**راه‌حل اصولی:**
در GitHub وارد آن پوشه شوید، همه فایل‌ها را انتخاب و به ریشه منتقل کنید.
یا ریپازیتوری را حذف کرده و این بار **محتوای داخل پوشه** را آپلود کنید، نه خود پوشه را.

> 💡 پروژه یک بررسی خودکار دارد: اگر ساختار اشتباه باشد، در لاگ Vercel
> پیام فارسی با نام دقیق پوشه اشتباه نمایش داده می‌شود.

#### خطاهای اتصال دیتابیس

| پیام خطا | علت و راه‌حل |
|----------|--------------|
| `password authentication failed` | رمز اشتباه است یا کاراکتر خاص encode نشده |
| `Tenant or user not found` | نام کاربری باید کامل باشد: `postgres.PROJECT-REF` |
| `ENOTFOUND` یا `ETIMEDOUT` | از آدرس مستقیم استفاده کرده‌اید؛ حتماً Pooler با پورت ۶۵۴۳ |
| `too many connections` | پورت را از ۵۴۳۲ به ۶۵۴۳ تغییر دهید |
| `permission denied` | رمز دیتابیس را از Supabase ریست کنید |

> پس از اصلاح متغیر در Vercel، حتماً از تب **Deployments** روی آخرین مورد
> **⋯ → Redeploy** بزنید تا مقدار جدید اعمال شود.

---

## مرحله ۵ — ورود به برنامه

```
https://نام-پروژه.vercel.app
```

| نام کاربری | رمز عبور |
|------------|----------|
| `admin`    | `123456` |

> 🚨 **بلافاصله** از مسیر **تنظیمات ← تغییر رمز عبور** رمز را عوض کنید.
> این آدرس عمومی است و هر کسی می‌تواند آن را باز کند.

---

## مرحله ۶ — اتصال دامنه شخصی (اختیاری)

1. در Vercel به مسیر **Settings → Domains** بروید.
2. ساب‌دامنه خود را وارد کنید، مثلاً `wallet.example.com`.
3. Vercel یک رکورد DNS به شما می‌دهد. در پنل دامنه‌تان ثبت کنید:

   | Type | Name | Value |
   |------|------|-------|
   | CNAME | `wallet` | `cname.vercel-dns.com` |

4. بعد از چند دقیقه، گواهی SSL به صورت خودکار صادر می‌شود.

---

## مرحله ۷ — نصب روی آیفون

1. آدرس را در **Safari** باز کنید (حتماً Safari، نه Chrome).
2. دکمه **Share** (مربع با فلش رو به بالا) را لمس کنید.
3. **Add to Home Screen** را انتخاب کنید.

حالا یک آیکون روی صفحه اصلی دارید که تمام‌صفحه و بدون نوار مرورگر باز می‌شود.

---

## 🔄 به‌روزرسانی در آینده

هر بار که کد را در GitHub تغییر دهید (push کنید)، Vercel **خودکار** نسخه جدید را می‌سازد.
نیازی به هیچ کار دستی نیست. داده‌های شما در Supabase دست‌نخورده باقی می‌مانند.

---

## 💾 پشتیبان‌گیری

### الف) از داخل برنامه
**تنظیمات ← پشتیبان‌گیری و بازگردانی**

- **JSON:** برای بازگردانی با دکمه Restore در خود برنامه
- **SQL:** برای اجرای مستقیم در SQL Editor سوپابیس

### ب) بک‌آپ خودکار روزانه (داخل خود برنامه) ✅
> ⚠️ **پلن رایگان Supabase هیچ بک‌آپ خودکاری ندارد** (فقط پلن Pro با ۲۵ دلار در ماه).

به همین دلیل برنامه خودش هر شب ساعت ۰۰:۳۰ (به وقت UTC) از طریق **Vercel Cron** یک اسنپ‌شات کامل
داخل دیتابیس ذخیره می‌کند و ۳۰ نسخه آخر را نگه می‌دارد. این کار **بدون هیچ تنظیمی** فعال است
(فایل `vercel.json` آن را تعریف کرده).

مسیر مشاهده و بازگردانی: **تنظیمات ← بک‌آپ خودکار روزانه**

**امنیت مسیر Cron (اختیاری ولی توصیه‌شده):**
در Vercel → Settings → Environment Variables یک متغیر بسازید:

| Name | Value |
|------|-------|
| `CRON_SECRET` | یک رشته تصادفی طولانی (مثلاً ۳۲ کاراکتر) |

Vercel این مقدار را خودکار برای Cron ارسال می‌کند و دیگران نمی‌توانند مسیر را دستی صدا بزنند.

> این اسنپ‌شات‌ها **داخل همان دیتابیس** هستند؛ یعنی در برابر اشتباه کاربر (حذف ناخواسته) محافظت می‌کنند،
> ولی اگر کل پروژه Supabase از بین برود، آن‌ها هم می‌روند. برای همین ماهی یک بار فایل JSON را دانلود کنید.

### ج) دانلود کامل با pg_dump
برای این کار از رشته اتصال **مستقیم** (پورت ۵۴۳۲) استفاده کنید:

```bash
pg_dump "postgresql://postgres.REF:PASSWORD@aws-0-REGION.pooler.supabase.com:5432/postgres" > backup.sql
```

---

## ⚠️ نکات مهم پلن رایگان Supabase

| محدودیت | توضیح |
|---------|-------|
| **توقف پس از ۷ روز بی‌فعالیتی** | اگر یک هفته کامل به برنامه سر نزنید، پروژه Pause می‌شود. با ورود به پنل Supabase و زدن **Restore** دوباره فعال می‌شود و داده‌ها از بین نمی‌روند. |
| حجم دیتابیس | ۵۰۰ مگابایت — برای صدها هزار تراکنش کافی است |
| پهنای باند | ۵ گیگابایت در ماه |

> 💡 برای جلوگیری از Pause شدن، کافی است هفته‌ای یک بار برنامه را باز کنید.

---

## 🧭 خلاصه مسیر

```
Supabase  →  ساخت پروژه  →  کپی رشته اتصال (پورت 6543)
    ↓
GitHub    →  آپلود کد (بدون node_modules و .env)
    ↓
Vercel    →  Import  →  ثبت DATABASE_URL  →  Deploy
    ↓
تست /api/health  →  ورود با admin/123456  →  تغییر رمز
    ↓
نصب روی آیفون از طریق Safari
```
