# چک‌لیست ایرادهای «کیف پول هوشمند» (نسخه ۲.۶.۳)

> **هدف:** فهرست مکتوب همه ایرادهای استخراج‌شده از بررسی کامل مخزن، برای رسیدگی «به‌وقتش».
> **نحوه استفاده:** هر مورد یک کد یکتا دارد (SEC/BUG/QLT/FEAT). هنگام رفع، ستون وضعیت را ✅ کنید و تاریخ/توضیح بنویسید.
> شدت‌ها: 🔴 حیاتی/بالا — 🟡 متوسط — ⚪ کم.

## خلاصه آمار

| دسته | تعداد | 🔴 | 🟡 | ⚪ |
|---|---|---|---|---|
| امنیت (SEC) | ۹ | ۷ | ۲ | ۰ |
| باگ و منطق (BUG) | ۱۰ | ۳ | ۵ | ۲ |
| کیفیت و تست (QLT) | ۶ | ۲ | ۳ | ۱ |
| کمبود قابلیت (FEAT) | ۱۲ | — | — | — |

---

## ۱. امنیت (SEC) — اولویت رسیدگی: اول

| کد | ایراد | شدت | فایل‌های مرتبط | جهت رفع پیشنهادی | وضعیت |
|---|---|---|---|---|---|
| SEC-01 | **لاگین فقط نمایشی است.** ورود با فلگ localStorage انجام می‌شود و هیچ API بررسی نشست نمی‌کند؛ هرکسی با دانستن آدرس به خواندن/حذف/بازگردانی کل دیتابیس دسترسی دارد | 🔴 | `src/app/page.tsx` و همه `src/app/api/*/route.ts` | نشست امن سمت سرور (کوکی httpOnly) + میدل‌ور احراز هویت روی همه APIها | ⬜ |
| SEC-02 | هش رمز ضعیف: salt ثابت + فقط ۱۰۰۰ تکرار PBKDF2 | 🔴 | `src/lib/auth-utils.ts` | مهاجرت به Argon2/bcrypt با salt یکتا + ارتقای هش در لاگین بعدی | ⬜ |
| SEC-03 | رمز پیش‌فرض `admin/123456` که در خود صفحه لاگین نمایش داده می‌شود؛ بدون محدودیت نرخ و قفل حساب | 🔴 | `src/components/LoginModal.tsx`، `src/app/api/auth/route.ts` | حذف نمایش رمز از UI + rate-limit + قفل موقت پس از ۵ تلاش + اجبار تغییر رمز در اول ورود | ⬜ |
| SEC-04 | فایل بکاپ JSON شامل جدول `users` با هش رمزهاست (نشت اطلاعات احراز هویت) | 🔴 | `src/app/api/backup/route.ts` | حذف users از بکاپ یا رمزنگاری بکاپ با رمز کاربر | ⬜ |
| SEC-05 | بازگردانی بکاپ بدون تراکنش: اول DELETE همه‌چیز، بعد INSERT؛ خطای وسط راه = از دست رفتن داده | 🔴 | `src/db/repo.ts` (`replaceAll`) | اجرای کل بازگردانی داخل یک تراکنش دیتابیسی + اعتبارسنجی قبل از حذف | ⬜ |
| SEC-06 | مبالغ مالی با `double` (اعشار شناور) ذخیره می‌شوند — خطای گردکردن در محاسبات پولی | 🔴 | `src/db/repo.ts` (DDL)، همه محاسبات | مهاجرت به عدد صحیح ریالی (BIGINT) یا DECIMAL | ⬜ |
| SEC-07 | بدون لاگ حسابرسی (نامشخص بودن عامل/زمان تغییرات) | 🔴 | — (نبود قابلیت) | جدول audit_log + ثبت ایجاد/ویرایش/حذف حساس | ⬜ |
| SEC-08 | اندپوینت `sms-intake` بدون توکن کاملاً باز است؛ توکن هم از query-string خوانده می‌شود (لو رفتن در لاگ) | 🟡 | `src/app/api/sms-intake/route.ts` | توکن اجباری در production + فقط هدر + rate-limit | ⬜ |
| SEC-09 | خروجی `health` شمارش حساب‌ها/تراکنش‌ها و نسخه را عمومی می‌دهد | 🟡 | `src/app/api/health/route.ts` | محدودسازی جزئیات یا محافظت با توکن داخلی | ⬜ |

## ۲. باگ و منطق (BUG) — اولویت رسیدگی: دوم

| کد | ایراد | شدت | فایل‌های مرتبط | جهت رفع پیشنهادی | وضعیت |
|---|---|---|---|---|---|
| BUG-01 | ایندکس‌ها فقط در نصب تازه ساخته می‌شوند؛ دیتابیس‌های قدیمی در مسیر «سریع» (`isAlreadyInitialized`) هرگز ایندکس نمی‌گیرند | 🔴 | `src/db/repo.ts` (`ensureDatabase`) | فراخوانی `ensureIndexes` در هر دو مسیر + تست روی DB قدیمی | ⬜ |
| BUG-02 | گردش حساب (`getLedger`) **همه** تراکنش‌های حساب را در حافظه می‌خواند تا مانده بگیرد — نقطه شکست با رشد داده | 🔴 | `src/db/repo.ts` (`getLedger`) | مانده تجمعی با window function در دیتابیس + صفحه‌بندی واقعی | ⬜ |
| BUG-03 | جستجو با `LIKE %...%` (درصد در ابتدا) از ایندکس استفاده نمی‌کند — مغایر با ادعای سرعت README | 🟡 | `src/db/repo.ts` (`buildTxWhere`) | جستجوی full-text یا حداقل مستندسازی محدودیت | ⬜ |
| BUG-04 | ناهمگامی `schema.ts` با DDL واقعی (فاقد `sort_order`، `status`، `source_hash` و جدول اسنپ‌شات) — ریسک مهاجرت اشتباه | 🟡 | `src/db/schema.ts` | یک منبع حقیقت برای اسکیما (ORM کامل یا DDL متمرکز) | ⬜ |
| BUG-05 | کش ۱۵ثانیه‌ای حساب‌ها فقط در حافظه همان نمونه است؛ بین نمونه‌های سرورلس ناهمگام و کهنه می‌خواند | 🟡 | `src/db/repo.ts` (`accountsCache`) | حذف کش یا انتقال به کش خارجی/کوتاه‌مدت با نسخه | ⬜ |
| BUG-06 | بازگردانی بکاپ، سلامت ارجاع‌ها را چک نمی‌کند — امکان تراکنش یتیم (حساب ناموجود) | 🟡 | `src/app/api/backup/route.ts` | اعتبارسنجی یکپارچگی قبل از جایگزینی + تراکنش (مرتبط با SEC-05) | ⬜ |
| BUG-07 | اسنپ‌شات‌های کامل (۳۰ نسخه) **داخل خود دیتابیس** ذخیره می‌شوند — بادکردن DB و کندی بکاپ هاست | 🟡 | `src/db/repo.ts` (snapshots) | فشرده‌سازی + نگهداری خارج از DB اصلی (فایل/آبجکت‌استوریج) | ⬜ |
| BUG-08 | تولید SQL بکاپ با الحاق رشته (هرچند escape دارد، شکننده است) | ⚪ | `src/app/api/backup/route.ts` (`sqlValue`) | استفاده از پارامتر/کتابخانه معتبر یا حذف فرمت SQL | ⬜ |
| BUG-09 | منطق تشخیص ترکیب مجاز مبدا/مقصد در دو جا تکرار شده (`deriveTxType` سرور و `isValidPair` کلاینت) — ریسک ناهمگامی | ⚪ | `src/app/api/transactions/route.ts`، `src/components/TransactionModal.tsx` | یک ماژول مشترک + تست | ⬜ |
| BUG-10 | در نصب تازه production هم داده نمایشی (حساب‌ها و تراکنش‌های نمونه) سید می‌شود | 🟡 | `src/db/repo.ts` (`seedInitialData`) | سید نمونه فقط در محیط توسعه؛ production با ویزارد شروع خالی | ⬜ |

## ۳. کیفیت، تست و تجربه فنی (QLT)

| کد | ایراد | شدت | فایل‌های مرتبط | جهت رفع پیشنهادی | وضعیت |
|---|---|---|---|---|---|
| QLT-01 | **هیچ تست خودکاری وجود ندارد** و CI هم نیست — هر تغییر ریسک شکستن مانده‌ها/گزارش‌ها را دارد | 🔴 | کل پروژه | تست موتور مالی (تراز/مانده/کارمزد) + CI روی گیت‌هاب | ⬜ |
| QLT-02 | اعتبارسنجی ورودی APIها دستی و پراکنده است (بدون zod/اسکیما) | 🔴 | همه `src/app/api/*/route.ts` | لایه اعتبارسنجی متمرکز با اسکیما | ⬜ |
| QLT-03 | پیام‌ها و تأییدها با `alert/confirm` مرورگر — تجربه ضعیف و غیرقابل‌استایل | 🟡 | `src/app/page.tsx` و مودال‌ها | سیستم toast + دیالوگ تأیید اختصاصی | ⬜ |
| QLT-04 | بدون Error Boundary و مدیریت خطای سراسری در فرانت — یک خطا = صفحه سفید | 🟡 | `src/app/*` | Error Boundary + صفحه خطای فارسی + لاگ | ⬜ |
| QLT-05 | «PWA» فقط در حد manifest/آیکون است؛ بدون Service Worker و آفلاین واقعی | 🟡 | `src/app/manifest.ts` | تصمیم: یا PWA واقعی یا حذف ادعا از مستندات | ⬜ |
| QLT-06 | همه state در `page.tsx` با prop-drilling عمیق — نگهداری سخت با رشد قابلیت‌ها | ⚪ | `src/app/page.tsx` | مدیریت state سبک (context/store) + جداسازی منطق داده | ⬜ |

## ۴. کمبودهای قابلیتی (FEAT) — مسیر رسیدگی: طبق PRD محصول جدید

این‌ها «ایراد» به معنای باگ نیستند؛ قابلیت‌هایی‌اند که کیف پول ندارد و در نقشه راه محصول جدید (فاز ۱ تا ۳) دیده شده‌اند. اینجا فقط فهرست می‌شوند تا چیزی از قلم نیفتد:

| کد | قابلیت | فاز PRD | وضعیت |
|---|---|---|---|
| FEAT-01 | بودجه‌بندی سرفصل‌ها + هشدار سقف | فاز ۱ | ⬜ |
| FEAT-02 | وام و اقساط + یادآور سررسید | فاز ۱ | ⬜ |
| FEAT-03 | تراکنش‌های دوره‌ای خودکار | فاز ۱ | ⬜ |
| FEAT-04 | نمودارها و داشبورد تحلیلی | فاز ۱ | ⬜ |
| FEAT-05 | خروجی Excel و PDF + چاپ تمیز | فاز ۱ | ⬜ |
| FEAT-06 | چندکاربره واقعی با نقش (مالک/حسابدار/مشاهده‌گر) | فاز ۱ | ⬜ |
| FEAT-07 | فاکتور خرید/فروش + ارزش افزوده | فاز ۲ | ⬜ |
| FEAT-08 | کالا و انبار (کاردکس، رسید/حواله) | فاز ۲ | ⬜ |
| FEAT-09 | چک دریافتنی/پرداختنی + سررسید | فاز ۲ | ⬜ |
| FEAT-10 | سند حسابداری چندسطر‌ی + کدینگ کامل | فاز ۳ | ⬜ |
| FEAT-11 | سال مالی، بستن دوره و قفل اسناد | فاز ۳ | ⬜ |
| FEAT-12 | صورت‌های مالی (تراز آزمایشی، سودوزیان، ترازنامه، جریان نقد) | فاز ۳ | ⬜ |

---

*مبنا: بررسی کامل مخزن در ۱۴۰۵/۰۶/۲۳ — این فایل با هر رسیدگی به‌روز می‌شود.*
