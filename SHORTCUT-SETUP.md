# 📱 راهنمای ستاپ شورتکات iOS — ثبت خودکار پیامک بانکی

این راهنما قدم‌به‌قدم نشان می‌دهد چطور روی آیفون خودتان شورتکاتی بسازید که پیامک بانکی را به کیف
پول هوشمند بفرستد تا تراکنش به‌صورت خودکار (با مبلغ، تاریخ و حساب بانکی درست) ثبت شود.

---

## 🔎 این سیستم چطور کار می‌کند؟

```
پیامک بانک می‌رسد (Messages)
      │
      │  ① متن پیامک را کپی می‌کنید (لمس طولانی روی پیامک ← Copy)
      │  ② شورتکات را اجرا می‌کنید (مثلاً دو ضربه پشت گوشی)
      │  ③ شرح تراکنش را تایپ می‌کنید (اختیاری — خالی هم می‌شود رد کرد)
      ▼
شورتکات iOS  ── POST ──▶  https://your-domain/api/sms-intake?format=text
      │                        │
      │                        ├─ پارس متن پیامک (واریز/برداشت، مبلغ، تاریخ، کارمزد)
      │                        ├─ تشخیص حساب بانکی از ۴ رقم آخر کارت/حساب
      │                        ├─ جلوگیری از ثبت تکراری
      │                        ▼
      │                  ساخت تراکنش «در انتظار دسته‌بندی» (با شرح شما)
      ▼
اعلان نتیجه: «✅ برداشت ۴٬۵۰۰٬۰۰۰ ریال از «بلو بانک» ثبت شد — در انتظار دسته‌بندی»
      │
      ▼  هر وقت فرصت کردید: در اپ، روی تراکنش بزنید ← سرفصل واقعی را انتخاب کنید ← ذخیره
```

**نکته مهم:** iOS اجازه نمی‌دهد هیچ اپی پیامک‌ها را در پس‌زمینه بخواند. به همین دلیل شورتکات
متن را از **کلیپ‌بورد** می‌خواند؛ یعنی کل کار روزمره شما این است: *کپی ← دو ضربه پشت گوشی. تمام.*

---

## ✅ پیش‌نیازها

| مورد | توضیح |
|------|-------|
| آدرس اپ | آدرس HTTPS سروری که این نسخه روی آن deploy شده (مثلاً `https://wallet.example.com`) — برای تست اولیه می‌توانید از لینک پیش‌نمایش همین محیط هم استفاده کنید |
| توکن | فقط اگر روی سرور متغیر `SMS_INTAKE_TOKEN` تنظیم شده باشد لازم است (از `.env` سرور ببینید) |
| اپ Shortcuts | اپ پیش‌فرض «میان‌برها» روی آیفون (Shortcuts) |

> ⚠️ آدرس باید `https` باشد؛ بدون SSL اعلان نتیجه نمایش داده نمی‌شود و آیفون ممکن است درخواست را بلاک کند.

---

## 🛠 گام ۱ — ساخت شورتکات (۴ اکشن، حدود ۳ دقیقه)

اپ **Shortcuts (میان‌برها)** را باز کنید:

1. تب **Shortcuts** → دکمه **+** (شورتکات جدید)
2. نام شورتکات را بگذارید: **«ثبت در کیف پول»**

### اکشن ۱ — پرسیدن شرح تراکنش

- `Ask for Input` را جستجو و اضافه کنید (در iOS فارسی: «درخواست ورودی»)
- روی فلش **⌄** بزنید و این‌طور تنظیم کنید:
  - **Prompt** (سوال): `شرح تراکنش؟`
  - **Input Type**: `Text`
  - **Default Answer**: خالی بماند
- این شرح به‌جای متن پیامک به‌عنوان شرح تراکنش ذخیره می‌شود. اگر چیزی ننویسید و Done بزنید،
  خودِ متن پیامک شرح خواهد بود — پس کاملاً اختیاری است.

### اکشن ۲ — دریافت کلیپ‌بورد

- روی نوار جستجوی اکشن‌ها بزنید و `Get Clipboard` را جستجو کنید (در iOS فارسی: «دریافت کلیپ‌بورد»)
- آن را انتخاب کنید

### اکشن ۳ — ارسال به سرور

- `Get Contents of URL` را جستجو و اضافه کنید (در iOS فارسی: «گرفتن محتوای URL»)
- روی فلش **⌄** کنار اکشن بزنید تا همه گزینه‌ها باز شود و این‌طور پر کنید:

| فیلد | مقدار |
|------|-------|
| **URL** | `https://YOUR-DOMAIN/api/sms-intake?format=text` |
| **Method** | `POST` |
| **Headers** | خالی بماند |
| **Request Body** | `JSON` |

- در بخش Request Body روی **«Add new field»** بزنید:
  - کلید اول: `text` — نوع **Text** — روی فیلد مقدار بزنید و از منوی **متغیرها (.magic variable)** گزینه **Clipboard** را انتخاب کنید
  - کلید دوم: `description` — نوع **Text** — متغیر **Provided Input** (خروجی اکشن اول) را انتخاب کنید
  - کلید سوم (فقط اگر توکن دارید): `token` — نوع **Text** — مقدار: رشته توکن خود را تایپ کنید

> 💡 اگر کار با JSON برایتان سخت بود: **Request Body** را روی `Form` بگذارید و همان فیلدها
> (`text` و `description` و `token`) را بسازید — سرور هر دو قالب را می‌فهمد.

> 💡 پارامتر `?format=text` باعث می‌شود پاسخ سرور یک جمله فارسی ساده باشد که مستقیماً در اعلان
> نمایش داده می‌شود (بدون JSON خام).

### اکشن ۴ — نمایش نتیجه

- `Show Notification` را جستجو و اضافه کنید (در iOS فارسی: «نمایش اعلان»)
- در فیلد متن، متغیر **Contents of URL** (خروجی اکشن دوم) را قرار دهید
- گزینه **Play Sound** را روشن بگذارید

### ذخیره

بالا-چپ **Done** را بزنید. شورتکات آماده است ✅

---

## 🚀 گام ۲ — روش اجرا (یکی یا چند مورد را انتخاب کنید)

### الف) ضربه دوتایی پشت گوشی (Back Tap) — پیشنهاد ما ⭐

سریع‌ترین روش؛ بدون باز کردن هیچ اپی:

1. **Settings ← Accessibility ← Touch ← Back Tap** (تنظیمات ← دسترسی‌پذیری ← لمس ← ضربه پشت)
2. **Double-Tap** را انتخاب کنید
3. شورتکات **«ثبت در کیف پول»** را از لیست انتخاب کنید

استفاده روزمره: پیامک را کپی کنید ← دو بار پشت گوشی بزنید ← اعلان نتیجه ✅

### ب) آیکون روی صفحه اصلی

در اپ Shortcuts، روی شورتکات بزنید ← **…** ← **Add to Home Screen** — یک آیکون اختصاصی می‌گیرید.

### ج) ویجت قفل صفحه (iOS 16+)

لمس طولانی صفحه قفل ← Customize ← ویجت Shortcuts ← انتخاب «ثبت در کیف پول»

### د) با صدا

«هی Siri، ثبت در کیف پول» — بعد از کپی پیامک.

---

## 🤖 گام ۳ (آزمایشی) — اجرای خودکار هنگام دریافت پیامک

iOS اجازه می‌دهد برای «دریافت پیام از فرستنده خاص» اتومیشن بسازید؛ ولی دو محدودیت دارد:

1. اپل برای تریگر پیام، معمولاً **اجرای بدون تأیید را نمی‌دهد** — یعنی یک اعلان می‌آید و باید
   **Run** را بزنید (همان یک تپ).
2. مطمئن نیستیم متن پیامک به شورتکات پاس داده شود — **این دقیقاً یکی از چیزهایی است که در همین
   فاز آزمایشی باید تست کنیم.**

طریقه ساخت:

1. اپ Shortcuts ← تب **Automation** ← **+**
2. **New Personal Automation** (اتومیشن شخصی جدید)
3. **Message** (پیام) را انتخاب کنید
4. **When I receive a message** ← در بخش **Sender is** شماره‌های فرستنده بانک‌های خود را وارد کنید
   (شماره‌هایی که پیامک بانکی از آن‌ها می‌آید)
5. **Next** ← اکشن‌های پیش‌فرض را حذف کنید ← شورتکات **«ثبت در کیف پول»** را از لیست اکشن‌ها اضافه کنید
6. اگر گزینه **Ask Before Running** دیدید، خاموش کنید (اگر اجازه نداد، روشن بماند — فقط یک تپ اضافه می‌کند)
7. **Done**

**نحوه تست:** از خودتان (یا یک دوست) بخواهید تراکنش کوچکی انجام دهد تا پیامک بانک بیاید. اگر اعلان
«✅ …» آمد و تراکنش درست بود، یعنی متن پیامک به شورتکات رسیده و این مسیر هم کار می‌کند 🎉
اگر اعلان آمد ولی تراکنش از **کلیپ‌بورد قبلی** ساخته شد، یعنی متن پاس نمی‌شود — در این حالت همان
روش‌های گام ۲ را استفاده کنید و نتیجه را به ما بگویید تا نسخه مخصوص اتومیشن را بدهیم.

---

## 🧪 گام ۴ — تست کامل

1. یکی از **پیامک‌های نمونه** صفحه «ثبت سریع از پیامک» در اپ را در یادداشت کپی کنید، یا یک پیامک
   واقعی بانک را کپی کنید (لمس طولانی روی پیامک ← Copy)
2. شورتکات را اجرا کنید (Back Tap یا آیکون)
3. باید این اعلان را ببینید:
   `✅ برداشت ۴٬۵۰۰٬۰۰۰ ریال از «بلو بانک (سامان)» ثبت شد — در انتظار دسته‌بندی.`
4. در اپ: صفحه اصلی ← تراکنش جدید با برچسب نارنجی **«در انتظار»**
5. روی آن بزنید ← سرفصل واقعی را انتخاب کنید ← ذخیره ← برچسب برداشته می‌شود و تراکنش «فعال» می‌شود
6. همان پیامک را دوباره بفرستید ← باید ببینید: `ℹ️ این پیامک قبلاً ثبت شده بود` (محافظ تکراری)

---

## 🔧 رفع اشکال

| اعلان / مشکل | معنی | راه‌حل |
|---|---|---|
| `⚠️ توکن ثبت سریع نامعتبر است.` | توکن در شورتکات با `SMS_INTAKE_TOKEN` سرور یکی نیست | مقدار دقیق را در فیلد `token` بدنه درخواست بگذارید |
| `⚠️ پیامک بانکی شناسایی نشد — مبلغ معتیری …` | متن پارس نشد | متن را دقیق و کامل کپی کنید؛ اگر پیامک واقعی بانک شما است، نمونه‌اش را برایمان بفرستید تا الگویش را اضافه کنیم |
| `⚠️ … حساب بانکی متناظری پیدا نشد` | ۴ رقم آخر کارت/حساب در هیچ حسابی ثبت نشده | در اپ، «ویرایش حساب» ← در **اطلاعات تکمیلی** بنویسید: `کارت: 6219-8610-4312-9023` (یا `حساب: 4788-901-12`) |
| `⚠️ چند حساب «…» دارید` | با نام بانک چند حساب دارید و شماره کارت هم در پیامک نبود | شماره کارت را در اطلاعات تکمیلی حساب‌ها ذخیره کنید تا با رقم تطبیق شود |
| `⚠️ … نوع تراکنش … مشخص نیست` | نه کلیدواژه‌ای از واریز/برداشت بود و نه «از/به …» | نمونه پیامک را برایمان بفرستید |
| `ℹ️ این پیامک قبلاً ثبت شده بود` | رفتار طبیعی محافظ تکراری (پنجره ۷ روزه) | نیازی به کار نیست |
| هیچ اعلانی نیامد | آدرس غلط / قطع اینترنت / `?format=text` فراموش شده | URL را از صفحه «ثبت سریع از پیامک» در اپ کپی کنید (دکمه کپی) |
| خطای `https` یا SSL | آدرس http است | روی سرور SSL فعال کنید (Let's Encrypt) |

---

## 🔐 نکات امنیتی

- **توکن:** اگر `SMS_INTAKE_TOKEN` روی سرور تنظیم شده باشد، هر درخواست باید همان توکن را داشته باشد
  (در بدنه JSON/Form، هدر `x-intake-token` یا کوئری `?token=`). برای تولید یک توکن قوی:
  `openssl rand -hex 24`
- **HTTPS:** آدرس حتماً `https` باشد؛ ترافیک پیامک شما (مبلغ و ارقام کارت) رمزنگاری‌شده می‌رود.
- بدون توکن، اندپوینت باز است — مثل بقیه APIهای فعلی برنامه. برای استقرار عملیاتی حتماً توکن بگذارید.
- متن پیامک به‌عنوان توضیح تراکنش ذخیره می‌شود و فقط در دیتابیس خودتان می‌ماند؛ هیچ سرویس
  third-party (تلگرام و غیره) در مسیر نیست.

---

## 🗺 بعد از تست چه می‌شود؟

اگر این نمونه جواب داد، فاز بعدی:

1. **جعبه انتخاب سرفصل داخل خود شورتکات** — بعد از ثبت، منویی از سرفصل‌های منتخب نشان داده می‌شود
   تا همان‌جا تراکنش کامل شود (بدون باز کردن اپ)
2. **یادگیری طرف مقابل** — اگر «کارت ****۹۰۲۳» همیشه به «شخص رضا» می‌رود، پیشنهاد خودکار
3. **صف «در انتظار تکمیل»** با بج شمارنده روی صفحه اصلی
4. **پشتیبانی از فرستنده‌های پیامک بانک‌های شما** — نمونه پیامک‌های واقعی بفرستید تا دقت پارسر
   برای بانک‌هایتان ۱۰۰٪ شود
5. در صورت تمایل: ربات تلگرام روی همین هسته

---

*این قابلیت آزمایشی است — نسخه اولیه برای تست سرتاسری جریان کار.*
